package Heranca.Calculadora;

public class Percentual {
    //Desenvolva uma nova classe que extende as funcionalidades da calculadora e que calcule o percentual de um número em relação a outro.
    public double percentual(double a, double b){
        return (a / b) * 100;
    }
}
